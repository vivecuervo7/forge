<!-- curator.md — snippet-authoring conventions, read by the forge curator.

Usually stays empty — the curator's defaults (single-concern snippets, kebab
names, args over inlined values, account-agnostic) cover most projects. Write
here only for genuine deviations: e.g. "treat process.env.LOCALE as required",
"compose multi-step flows rather than fusing them".

Empty file → curator defaults apply. See README.md in this folder. -->
