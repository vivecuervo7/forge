<!-- forge.md — the "operate the app" hint, read by the forge lead and driver.

Describe what only your project knows: env contract (key names + any loading
recipe), test accounts / roles, optional setup & teardown, app structure, route
map, canonical selectors, known gotchas, and any spec naming/verify deviations.

Empty file → forge uses its defaults (fine for simple, unauthenticated sites).
See README.md in this folder for what belongs here and a starter prompt. -->
